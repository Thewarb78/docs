kubectl exec \
    "$pod" \
    -n "${namespace}" \
    --kubeconfig "${common.k8s_cluster}-kubeconfig" \
    -- bash -c '
        pwd
        ls -l
        ss \
            --master ://... \
            ...
    '
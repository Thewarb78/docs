stage('Smoke Test Image') {
  steps {
    sh '''
      set -e

      set -a
      . "versions/${ENV_FILE}"
      set +a

      docker run --rm \
        --entrypoint bash \
        ${REMOTE_IMAGE_NAME}:${IMAGE_TAG} \
        /opt/spark/tests/smoke-test.sh
    '''
  }
}
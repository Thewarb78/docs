apiVersion: v1
kind: Pod
metadata:
  name: image-test
  namespace: spark   # change if needed
spec:
  restartPolicy: Never

  imagePullSecrets:
    - name: 

  containers:
    - name: spark
      image: 
      imagePullPolicy: Always

      command: ["sleep"]
      args: ["3600"]

      resources:
        requests:
          memory: "1Gi"
          cpu: "500m"
        limits:
          memory: "2Gi"
          cpu: "1"

      volumeMounts:
        - name: spark-work
          mountPath: /data/spark/work

  volumes:
    - name: spark-work
      emptyDir: {}
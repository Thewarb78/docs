AppProject: spark-platform
Application: spark-namespace-platform
Target namespace: spark

repo/
  environments/
    dev/
      spark/
        rbac.yaml
        history-server.yaml
        gateway.yaml
    stg/
      spark/
    prd/
      spark/

spark-k8s-platform

Kubernetes platform configuration for Spark-on-Kubernetes runtime components, including namespace resources, RBAC, service accounts, History Server, Gateway API configuration and related operational platform manifests managed via Argo CD.
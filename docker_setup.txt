post {
  always {
    sh '''
      set +e

      set -a
      . "versions/${ENV_FILE}"
      set +a

      docker image rm ${REMOTE_IMAGE_NAME}:${IMAGE_TAG} || true
      docker builder prune -f --filter until=24h || true
    '''
  }
}
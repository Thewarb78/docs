Hi Jorge,

Understood, thanks.

The issue for us is that the current process is fully automated through Ansible Tower using a dedicated automation account and is designed to run unattended.

If the model only permits temporary/on-demand admin access for individual user accounts, then this will create operational challenges for the existing automation model. The current implementation is not designed to execute under rotating individual developer credentials.

We can investigate possible adaptations from the automation side, however this is not a minor script change and would require rework of the current execution, credential handling, scheduling/unattended execution, auditability and long-term ownership approach.